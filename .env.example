# Demo App Environment Variables

# Required - Your OpenServ webhook trigger URL
OPENSERV_WEBHOOK_URL=https://api-staging.oserv.dev/webhooks/trigger/YOUR_WEBHOOK_ID

# Optional - URL where this app is running (used in OpenServ secrets as frontend_url)
# In production, this would be your deployed URL
# Locally, it's http://localhost:3000/api/callback
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional - Callback polling configuration
# Maximum time to wait for agent response (in milliseconds)
NEXT_PUBLIC_CALLBACK_TIMEOUT=60000

# How often to poll for callback (in milliseconds)
NEXT_PUBLIC_CALLBACK_POLL_INTERVAL=1000

# Polling Configuration Examples:
#
# Fast responses (simple prompts, quick agents):
#   NEXT_PUBLIC_CALLBACK_TIMEOUT=30000    # 30 seconds
#   NEXT_PUBLIC_CALLBACK_POLL_INTERVAL=500  # Check every 0.5s
#
# Standard responses (most use cases):
#   NEXT_PUBLIC_CALLBACK_TIMEOUT=60000    # 60 seconds (default)
#   NEXT_PUBLIC_CALLBACK_POLL_INTERVAL=1000  # Check every 1s (default)
#
# Slow responses (complex tasks, multiple API calls):
#   NEXT_PUBLIC_CALLBACK_TIMEOUT=180000   # 3 minutes
#   NEXT_PUBLIC_CALLBACK_POLL_INTERVAL=2000  # Check every 2s
#
# Very slow responses (data processing, long computations):
#   NEXT_PUBLIC_CALLBACK_TIMEOUT=300000   # 5 minutes
#   NEXT_PUBLIC_CALLBACK_POLL_INTERVAL=3000  # Check every 3s
